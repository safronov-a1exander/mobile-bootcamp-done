import 'package:flutter/material.dart';

class SunAndMoonWidget extends StatelessWidget {
  const SunAndMoonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}